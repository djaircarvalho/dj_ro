<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation" class = "alert alert-danger fade in">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :opening_date %>
    <%= f.date_select :opening_date, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :closing_date %>
    <%= f.date_select :closing_date, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :expected_value %>
    <%= f.text_field :expected_value, class: 'form-control' %>
  </div>
  
 <div class="form-group">
      <%= f.label :users %>
      <%= f.collection_select :user_ids, User.all, :id, :email, {},  {:multiple => true, :class=>'select2 form-control'} %> 
 <div>
  <div class="actions">
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>
